   # 予約システム導入マニュアル（GAS設定編）

   このマニュアルでは、予約システムの「頭脳」となるプログラム（GAS）を設定します。
   難しそうに見えますが、手順通りにコピー＆ペーストするだけで完了します。
   所要時間は **15分〜20分** です。

   ---

   ## 全体の流れ
   1. **Googleスプレッドシートを用意する**（予約台帳になります）
   2. **プログラムを貼り付ける**（これが自動化の正体です）
   3. **IDを設定する**（正しい場所と連携させます）
   4. **テスト実行と承認**（Googleに「私が使うツールです」と教えます）
   5. **公開（デプロイ）**（ホームページと繋ぎます）

   ---

   ## ステップ1：スプレッドシートの準備

   1. パソコンでGoogleドライブを開き、**「＋ 新規」** ＞ **「Google スプレッドシート」** を作成します。
   2. 左上のファイル名をわかりやすく変更します（例：`予約管理表`）。
   3. メニューバーにある **「拡張機能」** ＞ **「Apps Script」** をクリックします。
      → 別タブで「Apps Script」という画面が開きます。

   ---

   ## ステップ2：プログラムの貼り付け

   1. 「無題のプロジェクト」という画面が開きます。ここがプログラムを書く場所です。
   2. 元々書かれている `function myfunction() { ... }` という文字を、**すべて消して真っ白** にしてください。
   3. **別でお渡ししたテキストファイル「GAS_Code.js」の中身** を、すべてコピーしてここに貼り付けます。
      （※1行でも欠けると動かないので、「すべて選択」してコピーしてください）
   4. 画面上部の **「💾（ディスクのアイコン）」** をクリックして保存します。プロジェクト名は「予約システム」などでOKです。

   ---

   ## ステップ3：IDの入力（連携設定）

   プログラムとファイルを紐付けます。ここが一番の重要ポイントです！
   コード（プログラム）の**1行目～10行目あたり**を見てください。

   ### ① スプレッドシートIDの設定
   1. さきほど作った「スプレッドシート（予約管理表）」の画面に戻ります。
   2. ブラウザ上部のURL（アドレスバー）を見てください。
      `https://docs.google.com/spreadsheets/d/` **ここから** `/edit...` **の前まで** の英数字の羅列をコピーします。
      （例：`1ulngUkF1HaUb_OnAPC...` のような長い文字列です）
   3. コードの `var spreadsheetId = '...';` の `' '` の中身を、コピーしたIDに書き換えます。
      **注意：** `'`（シングルクォーテーション）を消さないように気をつけてください！

   ### ② カレンダーIDの設定
   1. Googleカレンダーの「設定」画面を開きます（左メニューのカレンダー名の横の「︙」＞「設定と共有」）。
   2. 下の方にある「カレンダーの統合」セクションにある **「カレンダーID」** をコピーします。
      （通常はメールアドレスか、`〜@group.calendar.google.com` で終わる文字列です）
   3. コードの `var calendarId = '...';` の `' '` の中身に貼り付けます。
      **💡 ヒント：** もし自分のメインカレンダーを使う場合は、細かいIDの代わりに単に `primary` と入力するだけでも動きます。

   入力が終わったら、もう一度 **「💾 保存」** を押してください。

   ---

   ## ステップ4：承認とテスト（最重要）

   Googleに「このプログラムを実行してもいいよ」と許可を与えます。

   1. 画面上のツールバーにあるリストボックス（「デバッグ」の左）から **「testAuth」** という項目を選択します。
      （なければコードの一番下の方にある `function testAuth()...` を探してカーソルを合わせてください）
   2. **「実行」** ボタンをクリックします。
   3. **「承認が必要です」** という画面が出たら、以下の手順で進めてください（怖がらなくて大丈夫です！）。
      - **「権限を確認」** をクリック
      - ご自身のGoogleアカウントを選択
      - ⚠️**「このアプリはGoogleで確認されていません」** と出た場合：
      左下の **「詳細」** という小さな文字をクリックし、その下に出てくる **「（プロジェクト名）に移動（安全ではないページ）」** をクリックします。
      - 最後に **「許可」** をクリックします。
   4. 画面下の実行ログに「実行完了」と出ればOKです。

   ---

   ## ステップ5：公開（デプロイ）

   最後に、このシステムをホームページから使えるようにします。

   1. 画面右上の青いボタン **「デプロイ」** ＞ **「新しいデプロイ」** をクリックします。
   2. **「種類の選択」**（歯車マーク）の隣にある「Webアプリ」が選択されていることを確認します（なければ選択）。
   3. 以下の設定になっているか **必ず** 確認してください：
      - **次のユーザーとして実行**：`自分（メールアドレス）`
      - **アクセスできるユーザー**：**`全員`**
      （⚠️ここが「全員」でないと、お客様が予約できません！）
   4. **「デプロイ」** ボタンをクリックします。
   5. **「ウェブアプリ」** の下に表示された URL（`https://script.google.com/...`）をコピーします。

   ---

   ## 完了

   コピーしたURLが、予約システムの「住所」になります。
   これをホームページに設定すれば完了です！

   お疲れ様でした！これでもう難しい設定はありません。

   ---

   ### 【補足】設定を変更した場合（ID変更など）
   もし後でコードを書き換えた場合は、保存するだけでは反映されません。
   必ず以下の手順で更新してください。

   1. **「デプロイ」** ＞ **「デプロイを管理」** をクリック。
   2. 右上の **「編集（鉛筆マーク）」** をクリック。
   3. **「バージョン」** のプルダウンから **「新バージョン」** を選択。
   4. **「デプロイ」** ボタンをクリック。

   これでURLを変えずに中身だけ更新できます。
