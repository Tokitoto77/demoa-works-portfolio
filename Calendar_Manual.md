                # 予約カレンダー運用マニュアル

                ホームページのスケジュール表に表示される「予約枠」の作り方ガイドです。
                普段使っているGoogleカレンダーに、少しルールを決めて入力するだけで自動連携されます。

                ---

                ## 予約枠（レッスン）の作り方

                ### 基本ルール
                カレンダーの「予定のタイトル」に入力した文字が、そのままホームページに表示されます。
                時間はカレンダーで設定した「開始〜終了」時間が反映されます。

                ### 1. レッスン名の入力（色分け）
                タイトルに以下のキーワードを含めると、ホームページ上で自動的に色分けされます。
                （キーワードはタイトルのどこに入っていてもOKです）

                - **マシン** と書くと → <span style="color:orange">オレンジ色（Machine）</span>
                - **ミックス** と書くと → <span style="color:green">緑色（Mix）</span>
                - それ以外（基本） → 水色（Basic）

                **入力例：**
                - `マシンピラティス`
                - `朝のミックスレッスン`
                - `ベーシック`

                ---

                ### 2. 定員（残席）の設定【重要】

                ここが自動化のポイントです！
                タイトルの**先頭**に、以下の書き方で「残りの席数」を入力してください。
                システムがこれを読み取って「◎・△・×」を表示し、予約が入ると自動で数字を減らします。

                | 書き方 | ホームページの表示 | 状態 |
                | :--- | :--- | :--- |
                | `【残3】マシンピラティス` | **△**（残りわずか） | 予約可 |
                | `【残1】ベーシック` | **△**（残りわずか） | 予約可 |
                | `【満】アドバンス` | **×**（満席） | **予約不可** |
                | `マシンピラティス` | **◎**（余裕あり） | 予約可・**残数変動なし** |

                **表記が変わる詳細ルール：**
                - **◎（余裕あり）**：タイトルに**残数（数字）がない**場合です。これが設定されていると、予約が入っても**残数は減りません**。
                - **△（残りわずか）**：タイトルに**「【残〇】」**が含まれる場合です。ここに入力された数字が予約可能数となります。
                - **×（満席）**：タイトルが**「【満】」**になった場合です。

                **入力時の注意：**
                - カッコは隅付き括弧 `【 】` または半角大括弧 `[ ]` が使えます。
                - 数字は**半角数字**（1, 2, 3...）を使ってください。

                ---

                ### 3. 自動更新の仕組み

                お客様から予約が入ると、Googleカレンダーのタイトルが自動的に書き換わります。

                #### パターンA：定員あり（カウントダウン）
                「【残〇】」と書いた場合は、予約が入るたびに数字が減っていきます。
                1. **初期状態：** `【残3】体験レッスン` （表示：△）
                2. **1件予約が入る** → 自動で `【残2】体験レッスン` に書き換わります。
                3. **最後の1席が埋まる** → 自動で `【満】体験レッスン` に書き換わり、予約ストップとなります（表示：×）。

                #### パターンB：定員なし（ずっと受付）
                数字を書かずに登録した場合は、カウントダウンされません。
                1. **初期状態：** `オンラインレッスン` （表示：◎）
                2. **予約が入る** → タイトルは変わらず `オンラインレッスン` のまま維持されます。

                ※定員オーバーを防ぎたい場合は、必ず最初から `【残〇】` と入力してください。

                ---

                ## よくある質問

                **Q. 予定を削除したらどうなりますか？**
                A. ホームページからもその枠が消えます。急な休講などの場合は、カレンダーから予定を削除するか、タイトルを `【満】休講` などに変更してください。

                **Q. 時間を変更したい場合は？**
                A. カレンダー上で時間をドラッグして変更すれば、ホームページの表示時間も変わります。

                **Q. 反映されません**
                A. ホームページの反映には数分かかる場合があります。また、ブラウザの「再読み込み（リロード）」を試してみてください。

                以上です。
        