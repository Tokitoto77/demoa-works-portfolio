# カレンダー設定＆登録マニュアル

お客様（店舗様）向けの、Googleカレンダー設定ガイドです。
この手順で「どのカレンダーの予定をホームページに表示するか」を決めて、実際に予約枠を作ることができます。

---

## 1. 連携用カレンダーのIDを取得する

普段使っている個人の予定（美容院や飲み会など）がホームページに出てしまうと大変です。
お店の予約枠専用の「新しいカレンダー」を作ることをおすすめします。

1. パソコンで [Googleカレンダー](https://calendar.google.com/) を開きます。
2. 画面左側の「他のカレンダー」の横にある **＋** ボタンをクリックし、**「新しいカレンダーを作成」** を選びます。
3. 名前（例：`HP予約管理用`）を入力して、青い **「カレンダーを作成」** ボタンを押します。
4. 左側のリストに作ったカレンダーが表示されるので、クリックします。
5. 「カレンダーの設定」画面が開くので、下の方にスクロールして **「カレンダーの統合」** という項目を探します。
6. そこにある **「カレンダーID」** をコピーします。
   （例：`c_188...group.calendar.google.com` のような長い文字列です）

---

## 2. システムにIDを登録する

1. 先ほど開いた **Apps Script** の画面（プログラムの画面）に行きます。
2. コードの**2行目**あたりに、以下のような記述があります。
   ```javascript
   var calendarId = 'xxxxxxxxxx@group.calendar.google.com'; 
   ```
3. この `' '` の中身を、先ほどコピーした**カレンダーID**に書き換えます。
   （間違って `'` を消さないように注意してください）
4. 上の「💾（保存）」ボタンを押します。
5. 右上の「デプロイ」＞「デプロイを管理」＞「編集（鉛筆マーク）」＞「新しいバージョン」を選んで「デプロイ」を押して更新します。

---

## 3. 予約枠（レッスン）の作り方

ホームページの「レッスンスケジュール」には、このカレンダーに入れた予定が自動で表示されます。
以下のルールでカレンダーに予定を入れてください。

### 【基本の書き方】
カレンダーの「予定のタイトル」に、レッスン名を書くだけです。

- **タイトル**: `ベーシックピラティス`
- **日時**: レッスンの開始時間〜終了時間

### 【種類の指定（色分け用）】
レッスン名の中に、以下のキーワードを含めると、ホームページ上で色が付きます。
自動判別しますので、カッコなどは不要です。

- **マシン** という言葉が入っている → オレンジ色（Machine）
- **ミックス** という言葉が入っている → 緑色（Mix）
- それ以外 → 水色（Basic）

例：「マシンピラティス」「早朝ミックスレッスン」

### 【空き状況の表示】
タイトルの **先頭** に、以下の記号をつけると「○ △ ×」が変わります。

- `【満】` または `[満]` → **×（満席）** と表示され、予約ボタンが押せなくなります。
- `【残1】` 〜 `【残3】` → **△（残りわずか）** と表示されます。
- 何も書かない → **◎（余裕あり）** と表示されます。

**記入例：**
- `ベーシックピラティス` → ◎
- `【残2】マシンピラティス` → △
- `【満】アドバンス` → ×（満席）

---

## 4. 実際の表示確認

カレンダーに適当な予定（例：明日の10時に「テストレッスン」）を入れて、
ホームページの「レッスンスケジュール」を見てみてください。
※反映まで数分かかる場合がありますが、基本的には再読み込み（リロード）すればすぐ出ます。

以上です。
